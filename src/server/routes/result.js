const express = require('express');
const router = new express.Router();
// const authMid = require('../middleware/auth');
// const resultController = require('../controller/result');

// router.post('/:type', authMid.isMarker, resultController.postResult);
// router.get('/:type', authMid.isSecret, resultController.getResult);
// router.delete('/:type', authMid.isSecret, resultController.removeTurn);

module.exports = router;
